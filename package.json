{
  "name": "life-game-emulator",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start:dev": "tsnd -r tsconfig-paths/register --inspect --transpile-only --ignore-watch node_modules --respawn src/shared"
  },
  "devDependencies": {
    "ts-node-dev": "^1.1.8",
    "tsconfig-paths": "^3.14.1",
    "typescript": "^4.6.3"
  }
}
